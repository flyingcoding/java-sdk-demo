# FISCO BCOS 多群组配置指南

## 概述

本文档说明如何为 FISCO BCOS Java SDK Demo 的多群组压测工具配置不同的节点地址。通过新的配置管理器，每个群组可以连接到不同的节点集群。

## 配置方式

### 方式一：使用默认配置（所有群组共享）

如果所有群组都连接到相同的节点，只需使用默认的 `config.toml` 文件：

```
dist/conf/config.toml
```

### 方式二：群组特定配置（推荐）

为每个群组创建独立的配置文件，命名规则为 `config-group{groupId}.toml`：

```
dist/conf/
├── config.toml                # 默认配置（作为后备）
├── config-group1.toml         # 群组 1 的专用配置
├── config-group2.toml         # 群组 2 的专用配置
└── config-group3.toml         # 群组 3 的专用配置
```

## 配置步骤

### 1. 准备证书文件

所有群组可以共享相同的证书文件：

```bash
# 拷贝节点证书到配置目录
cp -r ~/fisco/nodes/127.0.0.1/sdk/* dist/conf/
```

### 2. 创建群组配置文件

#### 方法A：从示例文件创建

```bash
# 为群组 1 创建配置
cp dist/conf/config-group1-example.toml dist/conf/config-group1.toml

# 为群组 2 创建配置
cp dist/conf/config-group2-example.toml dist/conf/config-group2.toml
```

#### 方法B：从默认配置复制

```bash
# 为群组 1 创建配置
cp dist/conf/config.toml dist/conf/config-group1.toml

# 为群组 2 创建配置
cp dist/conf/config.toml dist/conf/config-group2.toml
```

### 3. 修改节点地址

编辑每个群组的配置文件，设置对应的节点地址：

**config-group1.toml:**
```toml
[network]
# 群组 1 的节点地址
peers=["192.168.1.10:20200", "192.168.1.11:20201"]
```

**config-group2.toml:**
```toml
[network]
# 群组 2 的节点地址
peers=["192.168.1.20:20200", "192.168.1.21:20201"]
```

**config-group3.toml:**
```toml
[network]
# 群组 3 的节点地址
peers=["192.168.1.30:20200", "192.168.1.31:20201"]
```

## 使用示例

### 示例 1：使用默认配置（所有群组连接相同节点）

```bash
cd dist
# 群组 1,2 都使用 config.toml 中的节点
java -cp 'conf/:lib/*:apps/*' org.fisco.bcos.sdk.demo.perf.AutoParallelOkMultiGroupPerf 1,2 1000 10000 100
```

### 示例 2：使用群组特定配置（不同群组连接不同节点）

```bash
cd dist
# 群组 1 使用 config-group1.toml，群组 2 使用 config-group2.toml
java -cp 'conf/:lib/*:apps/*' org.fisco.bcos.sdk.demo.perf.AutoParallelOkMultiGroupPerf 1,2 1000 10000 100
```

运行时输出会显示每个群组使用的配置文件：

```
========== 多群组配置信息 ==========
  群组 1 : config-group1.toml
  群组 2 : config-group2.toml
====================================
```

### 示例 3：混合配置（部分群组使用特定配置）

```bash
# 创建群组 1 的特定配置
cp dist/conf/config.toml dist/conf/config-group1.toml
# 编辑 config-group1.toml，设置群组 1 的特定节点

# 运行压测
# 群组 1 使用 config-group1.toml
# 群组 2,3 使用默认的 config.toml
java -cp 'conf/:lib/*:apps/*' org.fisco.bcos.sdk.demo.perf.AutoParallelOkMultiGroupPerf 1,2,3 500 5000 100
```

## 配置优先级

对于每个群组，配置文件的查找顺序为：

1. **首先** 查找群组特定配置：`config-group{groupId}.toml`
2. **其次** 使用默认配置：`config.toml`
3. **最后** 如果都不存在，报错退出

## 实际部署场景

### 场景 1：开发环境（所有群组在本地）

使用默认配置 `config.toml`：
```toml
[network]
peers=["127.0.0.1:20200", "127.0.0.1:20201"]
```

### 场景 2：测试环境（不同群组在不同服务器）

- **群组 1** - 测试服务器 A：
  ```toml
  [network]
  peers=["10.0.1.10:20200", "10.0.1.11:20201"]
  ```

- **群组 2** - 测试服务器 B：
  ```toml
  [network]
  peers=["10.0.2.10:20200", "10.0.2.11:20201"]
  ```

### 场景 3：生产环境（多机构联盟链）

- **群组 1** - 机构 A 的节点：
  ```toml
  [network]
  peers=["node1.org-a.com:20200", "node2.org-a.com:20201"]
  ```

- **群组 2** - 机构 B 的节点：
  ```toml
  [network]
  peers=["node1.org-b.com:20200", "node2.org-b.com:20201"]
  ```

- **群组 3** - 共识节点（机构 A + B）：
  ```toml
  [network]
  peers=["consensus1.alliance.com:20200", "consensus2.alliance.com:20201"]
  ```

## 注意事项

1. **证书兼容性**：确保所有配置文件中的证书路径正确，且证书与对应节点匹配
2. **网络连通性**：运行前确保客户端能够访问配置的所有节点地址
3. **端口配置**：不同群组的节点可能使用不同的端口，请确认配置正确
4. **性能考虑**：不同群组连接到不同节点时，网络延迟可能不同，建议相应调整 TPS 参数

## 故障排查

### 问题 1：找不到群组配置文件

错误信息：
```
群组 2 的配置文件不存在！尝试查找: config-group2.toml 或 config.toml
```

解决方法：
- 创建 `config-group2.toml` 文件
- 或确保 `config.toml` 存在作为默认配置

### 问题 2：连接节点失败

错误信息：
```
警告：群组 1 初始化失败: Connection refused
```

解决方法：
- 检查 `config-group1.toml` 中的节点地址是否正确
- 确认节点是否正在运行
- 验证网络连通性：`telnet 192.168.1.10 20200`

### 问题 3：证书不匹配

错误信息：
```
SSL handshake failed
```

解决方法：
- 确保证书文件与节点匹配
- 检查证书路径配置是否正确
- 如果不同群组使用不同证书，需要在各自配置文件中指定正确的证书路径

## 扩展功能

### 使用快速压测工具

快速压测工具同样支持多群组配置：

```bash
# 使用快速压测，自动识别每个群组的配置
java -cp 'conf/:lib/*:apps/*' org.fisco.bcos.sdk.demo.perf.QuickParallelMultiGroupPerf 1,2,3
```

### 配置文件模板

可以创建一个模板文件 `config-template.toml`，然后基于模板快速生成多个群组配置：

```bash
#!/bin/bash
# 生成多个群组配置的脚本

for i in {1..5}; do
    cp config-template.toml config-group$i.toml
    # 使用 sed 替换节点地址
    sed -i "s/NODE_IP/192.168.1.$((10+i))/g" config-group$i.toml
done
```

## 总结

通过新的多配置文件支持，您可以：

1. ✅ 为每个群组配置不同的节点地址
2. ✅ 灵活管理多环境部署
3. ✅ 支持复杂的联盟链场景
4. ✅ 保持向后兼容（默认配置仍然有效）

这种设计既保证了灵活性，又维持了简单场景下的易用性。